package com.bikemanager.ui.theme

import androidx.compose.ui.text.font.FontFamily

/**
 * Android implementation of custom fonts for Premium Dark Automotive design.
 *
 * NOTE: Custom fonts are not yet installed.
 * Using system fonts as fallback until fonts are added.
 *
 * TODO: Implement custom font loading for Android
 * - Download fonts from Google Fonts
 * - Add to shared resources
 * - Load using Compose Multiplatform resource API
 */

/**
 * Archivo Black font family for titles and headers.
 * Currently using system font fallback.
 */
actual val ArchivoBlack: FontFamily = FontFamily.SansSerif

/**
 * DM Sans font family for body text and labels.
 * Currently using system font fallback.
 */
actual val DMSans: FontFamily = FontFamily.SansSerif
