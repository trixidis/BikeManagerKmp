package com.bikemanager.ui.theme

import androidx.compose.ui.text.font.FontFamily

/**
 * iOS implementation of custom fonts for Premium Dark Automotive design.
 *
 * TODO: Implement custom fonts for iOS
 * For now, using system fonts as fallback.
 */

/**
 * Archivo Black font family for titles and headers.
 * Using system font as temporary fallback for iOS.
 */
actual val ArchivoBlack: FontFamily = FontFamily.SansSerif

/**
 * DM Sans font family for body text and labels.
 * Using system font as temporary fallback for iOS.
 */
actual val DMSans: FontFamily = FontFamily.SansSerif
